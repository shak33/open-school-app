import { ViewWrapper } from '@/components/ViewWrapper/ViewWrapper.comp';

export default function TeachersListPage() {
  return (
    <ViewWrapper pageTitle="Teachers list">
      {'teachers list'}
    </ViewWrapper>
  )
}