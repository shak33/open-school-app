import { ViewWrapper } from '@/components/ViewWrapper/ViewWrapper.comp';

export default function DashboardPage() {
  return (
    <ViewWrapper pageTitle="Dashboard">
      {'dashboard'}
    </ViewWrapper>
  )
}