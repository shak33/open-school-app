import { ViewWrapper } from '@/components/ViewWrapper/ViewWrapper.comp';

export default function SettingsPage() {
  return (
    <ViewWrapper pageTitle="Settings">
      {'settings'}
    </ViewWrapper>
  )
}