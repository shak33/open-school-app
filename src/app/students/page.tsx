import { ViewWrapper } from '@/components/ViewWrapper/ViewWrapper.comp';

export default function StudentsListPage() {
  return (
    <ViewWrapper pageTitle="Students list">
      {'students list'}
    </ViewWrapper>
  )
}