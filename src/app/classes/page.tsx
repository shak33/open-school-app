import { ViewWrapper } from '@/components/ViewWrapper/ViewWrapper.comp';

export default function ClassesListPage() {
  return (
    <ViewWrapper pageTitle="Classes list">
      {'classes list'}
    </ViewWrapper>
  )
}